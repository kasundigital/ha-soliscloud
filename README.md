# HA SolisCloud

Home Assistant custom integration for the SolisCloud API, maintained by Kasun Indika.

This project is being prepared as a backward-compatible, HACS-installable continuation for existing `solis` entities so Home Assistant dashboards, Recorder history, and Grafana queries can keep using the same entity IDs.
